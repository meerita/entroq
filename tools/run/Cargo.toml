[package]
name = "entroq-run"
version = "0.0.0"
edition = "2024"
rust-version = "1.98.1"
license = "MIT OR Apache-2.0"
publish = false

# The runner records what a campaign did. It reads and writes JSON, and it digests the
# input set a result was measured against. Entroq owns neither primitive.
[dependencies]
serde_json = "1"
sha2 = "0.10"

[lints]
workspace = true
